# Python
.venv/
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
*.egg-info/

# Node
node_modules/
dist/
build/

# Database
*.db

# Local data (SQLite DB + Garmin OAuth tokens) — NEVER commit
data/

# Environment
.env
.env.*
!.env.example   # the template is committed; the rule above must not shadow it

# Connect IQ watch app — private signing key + build output, NEVER commit
watch/developer_key
watch/bin/
*.prg

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db